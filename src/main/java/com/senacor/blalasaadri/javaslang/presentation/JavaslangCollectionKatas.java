package com.senacor.blalasaadri.javaslang.presentation;

import javaslang.collection.List;

/**
 * @author Alasdair Collinson, Senacor Technologies AG
 */
public class JavaslangCollectionKatas {

    /**
     * This method should create a list with any 5 different numbers, each of them unique in the list.
     *
     * @return a list with exactly 5 unique numbers.
     */
    public List<Integer> createListWithFiveDifferentNumbers() {
        return null;
    }

    /**
     * This method should take a list and return a different list where only the first element is different.
     * In the new list, the first element should be 1 rather than 0.
     *
     * @param originalList the list which should not be changed
     * @return the new list with 1 rather than 0 as its first element
     */
    public List<Integer> createListWithDifferentFirstElement(List<Integer> originalList) {
        return null;
    }

    /**
     * This method should take a separator and a String vararg and should then join the Strings from the vararg.
     * So for example, if the separator {@code " & "} is passed and the Strings to be joined are {@code "Carrot", "Cucumber", "Banana"}, the result
     * should be {@code "Carrot & Cucumber & Banana"}.
     *
     * @param separator the String which should separate the vararg elements
     * @param strings vararg containing the Strings to be joined
     * @return the joined Strings
     */
    public String joinStrings(String separator, String... strings) {
        return null;
    }

}
